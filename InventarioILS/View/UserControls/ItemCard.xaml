<UserControl x:Class="InventarioILS.View.UserControls.ItemCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventarioILS.View.UserControls"
             mc:Ignorable="d" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="{Binding ProductCode, FallbackValue=code}" FontSize="20" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
        <Button Grid.Column="1" x:Name="EditItemBtn" Click="EditItemBtn_Click" Content="Editar" Style="{StaticResource RoundedButtonPrimary}"/>
        <Button Grid.Column="2" x:Name="RemoveItemBtn" Click="RemoveItemBtn_Click" Content="Eliminar" Style="{StaticResource RoundedButtonSecondary}" Margin="10,0,0,0"/>
    </Grid>
</UserControl>
