<UserControl x:Class="InventarioILS.View.UserControls.ItemCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventarioILS.View.UserControls"
             x:Name="itemCard"
             mc:Ignorable="d" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        
        <Button Grid.Column="1" x:Name="EditItemBtn" Click="EditItemBtn_Click" Width="30" Height="30" Style="{StaticResource RoundedButtonHighlightTextTransparent}">
            <TextBlock Text="✎" FontSize="16"/>
        </Button>
        
        <Button Grid.Column="2" x:Name="RemoveItemBtn" Click="RemoveItemBtn_Click" Width="30" Height="30" Style="{StaticResource RoundedButtonHighlightTextTransparent}">
            <TextBlock Text="🗑" FontSize="14"/>
        </Button>

        <StackPanel Orientation="Horizontal" Grid.Column="0" Height="30">
            <TextBlock Text="{Binding ItemPreset.ProductCode, ElementName=itemCard, FallbackValue=code}" FontSize="20" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding ItemPreset.Description, ElementName=itemCard, FallbackValue=Description}" FontSize="16" Foreground="{StaticResource ForegroundBrush}" Margin="10,0,0,0" VerticalAlignment="Center"/>
            <TextBlock Text="Cantidad: " FontSize="16" Foreground="Gray" Margin="10,0,0,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding ItemPreset.Quantity, ElementName=itemCard, FallbackValue=0}" FontSize="16" Foreground="Gray" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
