<UserControl x:Class="InventarioILS.View.UserControls.AddOrderPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventarioILS.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Nuevo Pedido" FontSize="22" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>

                <Button Grid.Column="2" Width="100" Height="30" Style="{StaticResource RoundedButton}" Background="{StaticResource AccentBrush}" FontWeight="Bold" Content="Confirmar" VerticalAlignment="Center" HorizontalAlignment="Right" Click="ConfirmBtn_Click"/>
            </Grid>

            <!-- Descripción grande con borde turquesa -->
            <TextBox x:Name="OrderDescriptionInput" Margin="0,0,0,10" MinHeight="28" MaxHeight="130" Style="{StaticResource RoundedInput}" AcceptsReturn="True" TextWrapping="Wrap"
             BorderBrush="{StaticResource AccentBrush}" Background="#222" Foreground="#E6E6E6"/>

            <ContentControl Grid.Row="1" x:Name="FormContainer"/>

            <ScrollViewer Margin="0,5" x:Name="ItemListView" Visibility="Collapsed" Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Hidden">
                <StackPanel>
                    <ItemsControl Background="{StaticResource BackgroundBrush}" ItemsSource="{Binding ItemList}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <local:ItemCard Tag="{Binding LocalIndexTag}" OnEdit="ItemCard_OnEdit" OnDelete="ItemCard_OnDelete" Margin="15,0"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <Button x:Name="AddNewItem" Click="AddNewItem_Click" Content="Agregar nuevo" Style="{StaticResource RoundedButtonPrimary}" Height="40" Width="200" FontWeight="Bold" Margin="0,5"/>
                </StackPanel>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</UserControl>
