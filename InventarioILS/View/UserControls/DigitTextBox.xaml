<UserControl x:Class="InventarioILS.View.UserControls.DigitTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventarioILS.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBox x:Name="DigitInput" 
                     MinHeight="28" 
                     Style="{StaticResource RoundedInput}" 
                     Background="{StaticResource BackgroundBrush}" 
                     BorderBrush="{StaticResource AccentBrush}" 
                     BorderThickness="1" 
                     Foreground="{StaticResource ForegroundBrush}" 
                     FontWeight="Bold"
                     PreviewTextInput="DigitInput_PreviewTextInput"
                     MaxLength="10"/>
        </StackPanel>
            
    </Grid>
</UserControl>
