<UserControl x:Class="InventarioILS.View.UserControls.QueryableComboBox"
             x:Name="queryableComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventarioILS.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="64.561" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock x:Name="TitleLabel" Background="Transparent" Text="{Binding Title, ElementName=queryableComboBox, FallbackValue=title}" Foreground="{StaticResource AccentForegroundBrush}"/>
            <ComboBox x:Name="ComboBox"
              Tag="N/A"
              Margin="0,4,0,8" 
              Height="28" 
              Style="{StaticResource BlackComboBox}" 
              Foreground="{StaticResource ForegroundBrush}"
              Background="{StaticResource BackgroundBrush}"
              BorderThickness="1" 
              IsReadOnly="True"
              IsEnabledChanged="ComboBox_IsEnabledChanged"
              ItemsSource="{Binding ItemsSource, Mode=OneWay}"
              SelectedItem="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" >
                            <TextBlock Text="{Binding Name}" Tag="{Binding Id}"/>
                            <TextBlock Text="{Binding Shorthand}" Foreground="Gray" Margin="10, 0"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
    </Grid>
</UserControl>
