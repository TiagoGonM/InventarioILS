<Window x:Class="InventarioILS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InventarioILS"
        xmlns:userControls="clr-namespace:InventarioILS.View.UserControls"
        mc:Ignorable="d"
        Title="Inventario Electrónica" Width="1720" Height="1040"
        Background="{StaticResource BackgroundBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Panel lateral -->
        <Border Grid.Column="0" Background="{StaticResource SecondaryBrush}" BorderBrush="#3FFFEFEF" BorderThickness="0,0,1,0">
            <StackPanel Margin="0,0,0,0" Background="{StaticResource SecondaryBrush}">
                <!-- Tabs -->
                <StackPanel Orientation="Horizontal" Margin="16,16,0,16" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button Content="Inventario" Style="{x:Null}" Foreground="White" Background="Transparent" BorderBrush="Transparent" FontWeight="Normal" Padding="8,4"/>
                    <Button Content="Pedidos" Style="{x:Null}" Foreground="#FF232323" Background="#FFEFEFEF" BorderBrush="Transparent" FontWeight="Bold" Padding="24,4" Margin="8,0,0,0"/>
                </StackPanel>
                <!-- Filtros -->
                <StackPanel Margin="16,0,16,0">
                    <TextBlock Text="Código" Margin="0,8,0,0" Foreground="{StaticResource AccentForegroundBrush}" />
                    <TextBox x:Name="ProductCodeInput" Margin="0,4,0,8" Height="28" Style="{StaticResource RoundedInput}" BorderBrush="{StaticResource AccentBrush}" Background="#FF232323" BorderThickness="1" TextChanged="ProductCodeInput_TextChanged" />

                    <TextBlock Text="Palabras Clave" Foreground="{StaticResource AccentForegroundBrush}" />
                    <TextBox x:Name="KeywordInput" Margin="0,4,0,8" Height="28"  Style="{StaticResource RoundedInput}" BorderBrush="{StaticResource AccentBrush}" Background="#FF232323" BorderThickness="1" TextChanged="KeywordInput_TextChanged" />

                    <TextBlock Text="Tipo" Foreground="{StaticResource AccentForegroundBrush}"/>
                    <ComboBox x:Name="ClassComboBox" Margin="0,4,0,8" Height="28" Foreground="{StaticResource ForegroundBrush}" BorderThickness="1" IsReadOnly="True" Background="{StaticResource SecondaryBrush}" SelectionChanged="ClassComboBox_SelectionChanged">
                        <ComboBoxItem Content="Insumo" Background="Black" Foreground="{StaticResource ForegroundBrush}"/>
                        <ComboBoxItem Content="Dispositivo" Foreground="{StaticResource ForegroundBrush}"/>
                    </ComboBox>
                    <TextBlock Text="Cantidad" Foreground="{StaticResource AccentForegroundBrush}"/>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="0" Foreground="White" FontSize="10" VerticalAlignment="Center"/>
                        <Slider Minimum="0" Maximum="1000" Width="180" Margin="8,0"/>
                        <TextBlock Text="1000" Foreground="White" FontSize="10" VerticalAlignment="Center"/>
                    </StackPanel>
                    <CheckBox Content="Mostrar elementos sin stock" Foreground="White" Margin="0,8,0,0"/>
                </StackPanel>
                <!-- Botones -->
                <StackPanel Margin="16,24,16,0">
                    <Button Height="48" Style="{StaticResource RoundedButton}" FontWeight="Bold" FontSize="18" Margin="0,0,0,8" BorderBrush="Transparent" Click="AddButton_Click"
                            Foreground="{StaticResource SecondaryBrush}" Background="{StaticResource AccentBrush}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="+" FontSize="25" FontWeight="Bold" Margin="0,0,8,4" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            <TextBlock Text="Añadir Item" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="CollapseSidebarBtn" Height="48" Style="{StaticResource RoundedButton}" FontWeight="Bold" FontSize="18" BorderBrush="Transparent" RenderTransformOrigin="0.5,0.5"
                            Background="#FF9BE8D6" Foreground="{StaticResource SecondaryBrush}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="←" FontSize="40" FontWeight="Bold" Margin="0,0,8,0" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            <TextBlock Text="Colapsar" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Tabla de datos -->
        <Grid Grid.Column="1" Background="{StaticResource BackgroundBrush}">
            <userControls:InventoryDataView x:Name="ItemView" />
            <!--<DataGrid x:Name="ItemView" CanUserAddRows="False" CanUserDeleteRows="False" Foreground="{DynamicResource ForegroundBrush}" RowBackground="#FF232323"
                      AlternatingRowBackground="#FF232323" GridLinesVisibility="None" Margin="0,0,0,0"
                      ColumnHeaderHeight="40" RowHeight="40" FontSize="16" HorizontalAlignment="Center"
                      Background="{StaticResource SecondaryBrush}" HeadersVisibility="Column" AutoGenerateColumns="False" HorizontalScrollBarVisibility="Visible">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Código" Binding="{Binding ProductCode}" FontWeight="Bold" Width="120" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Descripción" Binding="{Binding Description}" Width="*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Tipo" Binding="{Binding Type}" Width="120" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Estado" Binding="{Binding State}" Width="120"/>
                    <DataGridTextColumn Header="Cantidad" Binding="{Binding Quantity}" Width="40"/>
                    <DataGridTextColumn Header="Ubicación" Binding="{Binding Location}" Width="160"/>
                    <DataGridTextColumn Header="Notas" Binding="{Binding AdditionalNotes}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>-->
        </Grid>
    </Grid>
</Window>
