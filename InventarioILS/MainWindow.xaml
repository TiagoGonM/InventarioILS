<Window x:Class="InventarioILS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InventarioILS"
        mc:Ignorable="d"
        Title="Inventario Electrónica" Width="1720" Height="1040"
        Background="{StaticResource BackgroundBrush}">
    <Window.Resources>
        <!-- Reutilizo/defino estilos para encabezados y celdas -->
        <Style x:Key="DataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#232323"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="12,8"/>           
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Foreground" Value="#E6E6E6"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>

        <Style x:Key="DataGridRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="56"/>
        </Style>

        <!-- Botón confirmar (usar RoundedButton si existe, sino estilo sencillo) -->
        <Style x:Key="ConfirmButtonStyle" TargetType="Button" BasedOn="{StaticResource RoundedButton}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource SecondaryBrush}"/>
            <Setter Property="Padding" Value="14,6"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar (sin cambios funcionales, alineado al diseño) -->
        <Border Grid.Column="0" Background="{StaticResource SecondaryBrush}" BorderBrush="#3FFFEFEF" BorderThickness="0,0,1,0">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="16,16,0,16">
                    <Button Content="Inventario" Style="{x:Null}" Foreground="#A8A8A8" Background="Transparent" BorderBrush="Transparent" FontWeight="Normal" Padding="8,4"/>
                    <Button Content="Pedidos" Style="{x:Null}" Foreground="White" Background="Transparent" BorderBrush="Transparent" FontWeight="Bold" Padding="24,4" Margin="8,0,0,0"/>
                </StackPanel>

                <StackPanel Margin="16,0,16,0">
                    <TextBlock Text="Código" Margin="0,8,0,0" Foreground="{StaticResource AccentForegroundBrush}" />
                    <TextBox Margin="0,4,0,8" Height="28" Style="{StaticResource RoundedInput}" BorderBrush="{StaticResource AccentBrush}" Background="#FF232323" BorderThickness="1" />

                    <TextBlock Text="Palabras Clave" Foreground="{StaticResource AccentForegroundBrush}" />
                    <TextBox Margin="0,4,0,8" Height="28"  Style="{StaticResource RoundedInput}" BorderBrush="{StaticResource AccentBrush}" Background="#FF232323" BorderThickness="1" />

                    <TextBlock Text="Tipo" Foreground="{StaticResource AccentForegroundBrush}"/>
                    <ComboBox Margin="0,4,0,8" Height="28" BorderThickness="1" BorderBrush="#6AC7B2" Background="#FF1D1C1C"
                              Foreground="{StaticResource ForegroundBrush}">
                        <ComboBoxItem Content="Insumo" IsSelected="True"/>
                        <ComboBoxItem Content="Dispositivo"/>
                    </ComboBox>

                    <TextBlock Text="Cantidad" Foreground="{StaticResource AccentForegroundBrush}" />
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="0" Foreground="White" FontSize="10" VerticalAlignment="Center"/>
                        <Slider Minimum="0" Maximum="1000" Width="180" Margin="8,0"/>
                        <TextBlock Text="1000" Foreground="White" FontSize="10" VerticalAlignment="Center"/>
                    </StackPanel>

                    <CheckBox Content="Mostrar elementos sin stock" Foreground="White" Margin="0,8,0,0"/>
                </StackPanel>

                <StackPanel Margin="16,24,16,0">
                    <Button Height="48" Style="{StaticResource RoundedButton}" FontWeight="Bold" FontSize="18" Margin="0,0,0,8" BorderBrush="Transparent" Click="Cancelar_Click"
                            Foreground="{StaticResource SecondaryBrush}" Background="{StaticResource AccentBrush}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="−" FontSize="22" FontWeight="Bold" Margin="0,0,8,4" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            <TextBlock Text="Cancelar" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button x:Name="CollapseSidebarBtn" Height="48" Style="{StaticResource RoundedButton}" FontWeight="Bold" FontSize="18" BorderBrush="Transparent" RenderTransformOrigin="0.5,0.5"
                            Background="#FF9BE8D6" Foreground="{StaticResource SecondaryBrush}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="←" FontSize="30" FontWeight="Bold" Margin="0,0,8,0" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            <TextBlock Text="Colapsar" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Área principal dividida: tabla arriba, editor/pedido abajo -->
        <Grid Grid.Column="1" Background="{StaticResource BackgroundBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="2.2*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>

            <!-- Tabla superior con borde turquesa alrededor y cabecera oscura -->
            <Border Grid.Row="0" Margin="12" BorderBrush="{StaticResource AccentBrush}" BorderThickness="4" Background="{StaticResource SecondaryBrush}" Padding="0">
                <DataGrid AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False" CanUserDeleteRows="False"
                          RowBackground="{StaticResource SecondaryBrush}" AlternatingRowBackground="{StaticResource SecondaryBrush}" GridLinesVisibility="None"
                          ColumnHeaderHeight="48" RowHeight="56" FontSize="16"
                          SelectionChanged="DataGrid_SelectionChanged"
                          Background="Transparent"
                          ColumnHeaderStyle="{StaticResource DataGridHeaderStyle}"
                          CellStyle="{StaticResource DataGridCellStyle}"
                          RowStyle="{StaticResource DataGridRowStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Código" Binding="{Binding ProductCode}" FontWeight="Bold" Width="120"/>
                        <DataGridTextColumn Header="Descripción" Binding="{Binding Description}" Width="*"/>
                        <DataGridTextColumn Header="Tipo" Binding="{Binding Type}" Width="120"/>
                        <DataGridTextColumn Header="Estado" Binding="{Binding State}" Width="120"/>
                        <DataGridTextColumn Header="Cant." Binding="{Binding Quantity}" Width="80"/>
                        <DataGridTextColumn Header="Ubicación" Binding="{Binding Location}" Width="160"/>
                        <DataGridTextColumn Header="Notas" Binding="{Binding AdditionalNotes}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Separador turquesa (línea entre tablas) y sección inferior -->
            <Border Grid.Row="1" Margin="12" BorderBrush="{StaticResource AccentBrush}" BorderThickness="0,3,0,0" Background="Transparent" Padding="12">
                <StackPanel>

                    <!-- Encabezado de la sección: Nuevo Pedido | Pedido #3 | Confirmar -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Nuevo Pedido" FontSize="22" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="Pedido #3" FontSize="20" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                        <Button Grid.Column="2" Style="{StaticResource ConfirmButtonStyle}" Content="Confirmar" VerticalAlignment="Center" HorizontalAlignment="Right" Click="Confirmar_Click"/>
                    </Grid>

                    <!-- Descripción grande con borde turquesa -->
                    <TextBox Height="88" Style="{StaticResource RoundedInput}" AcceptsReturn="True" TextWrapping="Wrap"
                             BorderBrush="{StaticResource AccentBrush}" Background="#222" Foreground="#E6E6E6" Padding="12" />

                    <!-- Espacio -->
                    <Rectangle Height="12" Fill="Transparent"/>

                    <!-- Contenedor de lista de items del pedido dentro de borde turquesa -->
                    <Border BorderBrush="{StaticResource AccentBrush}" BorderThickness="3" CornerRadius="4" Padding="12" Background="{StaticResource SecondaryBrush}">
                        <DataGrid x:Name="OrderItemsGrid"
                                  ItemsSource="{Binding OrderItems}" AutoGenerateColumns="False"
                                  CanUserAddRows="True" CanUserDeleteRows="True" IsReadOnly="False"
                                  GridLinesVisibility="None"
                                  ColumnHeaderHeight="44" RowHeight="48" FontSize="14"
                                  Background="Transparent"
                                  ColumnHeaderStyle="{StaticResource DataGridHeaderStyle}"
                                  CellStyle="{StaticResource DataGridCellStyle}"
                                  RowStyle="{StaticResource DataGridRowStyle}"
                                  RowEditEnding="OrderItemsGrid_RowEditEnding"
                                  CellEditEnding="OrderItemsGrid_CellEditEnding">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Código" Binding="{Binding Code, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                                <DataGridTextColumn Header="Categoría" Binding="{Binding Category, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="160"/>
                                <DataGridTextColumn Header="Subcategoría" Binding="{Binding Subcategory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="160"/>
                                <DataGridTextColumn Header="Descripción" Binding="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                                <DataGridTextColumn Header="Tipo" Binding="{Binding Type, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                                <DataGridTextColumn Header="Cant." Binding="{Binding Quantity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="70"/>

                                <DataGridTemplateColumn Header="">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="+" Width="28" Height="28" Click="AddQuantity_Click"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellStyle>
                                        <Style TargetType="DataGridCell">
                                            <Setter Property="Padding" Value="6"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                            <Setter Property="Background" Value="Transparent"/>
                                        </Style>
                                    </DataGridTemplateColumn.CellStyle>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
